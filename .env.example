# FITZONE FITNESS — Environment Configuration Template
# Copy this file to .env.local for local overrides.
# NEVER commit real credentials or secrets to version control.

# Site URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional Email Provider Configuration (For future production integrations)
# RESEND_API_KEY=
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASSWORD=
# ADMIN_EMAIL_RECIPIENT=join@fitzonefitness.com

# Optional CRM / Database Webhooks (For future production integrations)
# DATABASE_URL=
# SLACK_LEADS_WEBHOOK_URL=
# ZAPIER_LEADS_WEBHOOK_URL=
